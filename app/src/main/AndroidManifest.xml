<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rajukumarsingh.bloodbank">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Donor_login" />
        <activity android:name=".Donor" />
        <activity android:name=".Acceptor" />
        <activity android:name=".Donor_signup" />
        <activity android:name=".Bloodbank_list" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Blood_Bank" />
        <activity android:name=".Acceptor_SignUp" />
        <activity android:name=".Acceptor_Login" />
        <activity android:name=".Bloodbank_sign_up" />
        <activity android:name=".Bloodbank_login" />
        <activity android:name=".Donar_signup2" />
        <activity android:name=".Donar_signup3" />
        <activity android:name=".Blood_bank_details" />
        <activity android:name=".Acceptor_2" />
        <activity android:name=".Donor_list" />

        <receiver android:name=".VolumeChangeReceiver">
            <intent-filter>
                <action android:name="android.media.VOLUME_CHANGED_ACTION" />
            </intent-filter>
        </receiver>

        <activity android:name=".Offline"></activity>
    </application>

</manifest>